using RimWorld;
using System.Collections.Generic;
using Verse;

namespace MousekinRace
{
    /*public class ITab_ContentsCellar : ITab_ContentsBase
    {
        public List<Thing> listInt = new List<Thing>();

        public override IList<Thing> container
        {
            get
            {
                Building_StorageCellar cellar = base.SelThing as Building_StorageCellar;
                listInt.Clear();
                if (cellar != null && cellar.ContainedThing != null)
                {
                    listInt.Add(cellar.ContainedThing);
                }
                return listInt;
            }
        }

        public ITab_ContentsCellar()
        {
            labelKey = "TabCasketContents";
            containedItemsKey = "TabCasketContents";
        }
    }*/
}
